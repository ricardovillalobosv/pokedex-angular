import { SerializationPipe } from './serialization-pipe';

describe('SerializationPipe', () => {
  it('create an instance', () => {
    const pipe = new SerializationPipe();
    expect(pipe).toBeTruthy();
  });
});
